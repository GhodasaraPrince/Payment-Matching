FROM mcr.microsoft.com/dotnet/sdk:10.0 AS build
WORKDIR /src

COPY . .
RUN dotnet restore src/PaymentsMatchingTool.Api/PaymentsMatchingTool.Api.csproj
RUN dotnet publish src/PaymentsMatchingTool.Api/PaymentsMatchingTool.Api.csproj -c Release -o /app/publish --no-restore

FROM mcr.microsoft.com/dotnet/aspnet:10.0 AS runtime
WORKDIR /app
COPY --from=build /app/publish .

ENTRYPOINT ["dotnet", "PaymentsMatchingTool.Api.dll"]
